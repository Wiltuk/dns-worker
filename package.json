{
    "name": "dns",
    "private": true,
    "version": "1.0.0",
    "description": "Returns DNS entries for a given domain",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "format": "prettier --write '**/*.{js,css,json,md}'"
    },
    "author": "Adrian Hardy",
    "license": "ISC",
    "devDependencies": {
        "prettier": "^1.17.0"
    },
    "dependencies": {
        "itty-router": "^2.1.9",
        "serverless-cloudflare-workers": "^1.2.0",
        "xss": "^1.0.10"
    }
}
